<h1>Listing columns</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Position</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @columns.each do |column| %>
  <tr>
    <td><%= column.title %></td>
    <td><%= column.position %></td>
    <td><%= link_to 'Show', board_column %></td>
    <td><%= link_to 'Edit', edit_board_column_path([@board, column]) %></td>
    <td><%= link_to 'Destroy', [@board, column], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Column', "history.back()" %>
